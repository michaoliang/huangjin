# 黄金分析系统 - 架构设计文档

> 版本: v3.0 便携版  
> 创建时间: 2026-09-09  
> 作者: Agnes (Sapiens AI)

---

## 一、项目背景与目标

### 1.1 业务背景
用户拥有 MetaTrader 5 (EXNESS) 交易终端，持有黄金(XAUUSDc)仓位，希望通过自动化工具辅助交易决策。

### 1.2 核心目标
- 实时监测黄金行情与技术指标
- 提供买入/卖出信号建议
- 快速执行交易操作
- 追踪账户资金目标（从 $324 到 $2000）

### 1.3 便携性要求
- 软件可复制到任意电脑使用
- 自动检测 MT5 终端路径
- 配置文件独立存储
- 无需安装，即开即用

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     桌面快捷方式层                           │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  黄金分析助手.lnk │  │   黄金监控.lnk    │                │
│  └────────┬─────────┘  └────────┬─────────┘                │
│           │                     │                          │
└───────────┼─────────────────────┼──────────────────────────┘
            │                     │
┌───────────▼─────────────────────▼──────────────────────────┐
│                     Python 应用层                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              黄金分析助手.py (主程序)                   │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │  tkinter    │  │ matplotlib  │  │ MetaTrader5 │  │  │
│  │  │   GUI       │  │   图表      │  │    API      │  │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │  │
│  │         └────────────────┼─────────────────┘         │  │
│  │                    ┌────▼────┐                       │  │
│  │                    │ Analyzer│ ← 核心分析引擎        │  │
│  │                    └────┬────┘                       │  │
│  │                         │                           │  │
│  │    ┌────────────────────┼────────────────────┐      │  │
│  │    │                    │                    │      │  │
│  │ ┌──▼──┐           ┌───▼───┐           ┌───▼──┐ │      │  │
│  │ │价格 │           │技术指标│           │交易  │ │      │  │
│  │ │面板 │           │计算模块│           │执行  │ │      │  │
│  │ └─────┘           └───────┘           └──────┘ │      │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            │
┌───────────────────────────▼───────────────────────────────┐
│                     配置管理层                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           config.ini + scripts/                      │  │
│  │  • detect_mt5.py      → 自动检测MT5路径             │  │
│  │  • config_loader.py   → 读取配置文件                │  │
│  │  • config.ini         → 用户配置（可移植）           │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                            │
┌───────────────────────────▼───────────────────────────────┐
│                     MT5 连接层                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           MetaTrader5 Python 包                      │  │
│  │  • initialize(path)     → 连接终端                  │  │
│  │  • symbol_info_tick()   → 实时报价                  │  │
│  │  • copy_rates_from_pos()→ K线数据                   │  │
│  │  • account_info()       → 账户信息                  │  │
│  │  • positions_get()      → 持仓列表                  │  │
│  │  • order_send()         → 下单交易                  │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                            │
┌───────────────────────────▼───────────────────────────────┐
│                     终端实例层                             │
│  D:\MetaTrader 5 EXNESS\terminal64.exe                    │
│  账号: Exness-MT5Real40 #417309810                        │
│  端口: MCP 22345 (MetaEditor), 22346 (MetaTrader)          │
└────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 Analyzer 类（分析引擎）

**职责**: 封装所有 MT5 操作和技术分析逻辑

```python
class Analyzer:
    # 数据获取
    get_tick(symbol)        → 实时报价
    get_rates(symbol, tf, n)→ K线数据 (返回 numpy array)
    get_account()           → 账户信息
    get_positions(symbol)   → 持仓列表
    send_order(sym, act, lot) → 下单
    
    # 技术指标
    calc_ma(closes, period)     → 移动平均线
    calc_rsi(closes, period)    → 相对强弱指标
    calc_macd(closes)           → MACD 指标
    calc_bollinger(closes)      → 布林带
    calc_atr(highs, lows, c)    → 平均真实波幅
    calc_levels(rates)          → 支撑阻力位识别
    analyze(sym, tf)            → 综合分析（返回完整报告）
```

### 3.2 App 类（GUI 应用）

**职责**: 构建界面、处理用户交互、驱动刷新循环

```python
class GoldAnalyzerApp:
    # 界面布局 (1300x860 暗黑主题)
    _build_ui()               → 整体布局
    _panel_prices()           → 左上: 多品种实时价格
    _panel_signal()           → 左中: 信号分析 + 快速交易按钮
    _panel_account()          → 左下: 账户信息 + 持仓详情
    _panel_chart()            → 右上: K线图 + 周期切换
    _panel_indicators()       → 右下: 技术指标数值
    
    # 刷新逻辑
    _refresh()                → 全量刷新
    _auto_refresh()           → 3秒定时器
    _update_prices()          → 更新行情
    _update_account()         → 更新账户
    _update_signal()          → 更新信号
    _update_chart()           → 重绘K线图
    _update_target()          → 更新目标进度
```

### 3.3 信号分析算法

```
买入评分 += 以下条件满足时的权重:
  • MA5 > MA10 > MA20 (多头排列)    [+2]
  • RSI < 30 (超卖)                [+2]
  • RSI < 40 (偏多)                [+1]
  • MACD > 0 且 histogram > 0      [+1]
  • 价格 < 布林下轨                [+1]
  • 价格接近支撑位                  [+1]

卖出评分 += 以下条件满足时的权重:
  • MA5 < MA10 < MA20 (空头排列)    [+2]
  • RSI > 70 (超买)                [+2]
  • RSI > 60 (偏空)                [+1]
  • MACD < 0 且 histogram < 0      [+1]
  • 价格 > 布林上轨                [+1]
  • 价格接近阻力位                  [+1]

综合判断:
  buy_score - sell_score > +2  → 强烈买入 (绿色)
  buy_score > sell_score       → 偏多 (浅绿)
  sell_score - buy_score > +2  → 强烈卖出 (红色)
  sell_score > buy_score       → 偏空 (橙色)
  其他                         → 观望 (灰色)
```

---

## 四、数据流

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ MT5终端  │───▶│ Analyzer │───▶│  信号    │───▶│  界面    │
│ (实时数据)│    │ (分析计算)│    │ (评分系统)│    │ (展示)   │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                        │
                        ▼
                  ┌──────────┐
                  │  决策建议 │
                  │ 买入/卖出 │
                  └──────────┘

配置管理:
  config.ini ← detect_mt5.py → 自动检测MT5路径
       ↓
  config_loader.py → 读取配置 → Analyzer初始化
```

**刷新频率**: 每 3 秒自动刷新一次

**关键数据链路**:
1. `mt5.symbol_info_tick()` → 实时价格
2. `mt5.copy_rates_from_pos()` → K线数据 (H1/M15/M5/D1)
3. `mt5.account_info()` → 账户余额/净值
4. `mt5.positions_get()` → 当前持仓

---

## 五、界面布局

```
┌────────────────────────────────────────────────────────────────┐
│ 📊 黄金分析助手 v2.2                        ● 已连接          │
│                                    目标 $2000 | 当前 $324...   │
├─────────────────────────┬──────────────────────────────────────┤
│ 📈 实时行情             │ 📉 K线图表                           │
│ ┌─────────────────────┐ │ ┌────────────────────────────────┐  │
│ │ 黄金 (XAUUSDc) 4397  │ │ │                                │  │
│ │ 欧元/美元 (EUR...) 1 │ │ │     matplotlib K线图表         │  │
│ │ 美元/日元 (USD...) 1 │ │ │     (蜡烛图 + MA + 布林带)     │  │
│ │ 比特币 (BTC...) 78484│ │ │                                │  │
│ └─────────────────────┘ │ └────────────────────────────────┘  │
├─────────────────────────┼──────────────────────────────────────┤
│ 🎯 分析信号             │ 📐 技术指标                         │
│ ┌─────────────────────┐ │ ┌──────┬──────┬──────┬──────┬─────┐ │
│ │   观望              │ │ │MA5 │MA10│MA20│MA50│ RSI│ ... │ │
│ │                     │ │ ├──────┼──────┼──────┼──────┼─────┤ │
│ │ • MA短期向下: 中性  │ │ │4402│4400│4411│4415│ 38 │ ... │ │
│ │ • RSI=38: 偏多      │ │ └──────┴──────┴──────┴──────┴─────┘ │
│ │ • MACD金叉: 买入    │ │                                     │
│ └─────────────────────┘ │ 波动率(ATR): 0.39% (中)             │
│ ┌─────────────────────┐ │                                     │
│ │ XAUUSDc 快速交易:   │ │ 支持周期切换: M1 M5 M6 M15 H1 H4 D1       │
│ │ [买入0.1手] [卖出0.1]│ │                                     │
│ │ [买入0.5手] [卖出0.5]│ │ 一键交易按钮                        │
│ └─────────────────────┘ │                                     │
│ ┌─────────────────────┐ │                                     │
│ │ 当前持仓            │ │                                     │
│ │ 做多 0.5手 @ 4406.2 │ │                                     │
│ │ 亏损 $101.45        │ │                                     │
│ └─────────────────────┘ │                                     │
├─────────────────────────┴──────────────────────────────────────┤
│ 💰 账户信息                                                   │
│   余额: $384.70  净值: $280.80  保证金: $112.54  可用: $112.54 │
│   浮动盈亏: $-101.45                                            │
└────────────────────────────────────────────────────────────────┘
```

---

## 六、技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 语言 | Python 3.12 | 主开发语言 |
| GUI | tkinter | 桌面界面 |
| 图表 | matplotlib | K线图渲染 |
| 交易接口 | MetaTrader5 (官方包) | MT5 终端通信 |
| 数据分析 | numpy | 数值计算 |
| 配置管理 | configparser | 便携配置 |

---

## 七、便携性设计

### 7.1 自动检测机制
```python
# detect_mt5.py - 自动查找MT5终端
def detect_terminal():
    # 1. 扫描常见安装路径
    # 2. 扫描 AppData 中的终端实例
    # 3. 尝试连接并验证账户
    # 4. 返回第一个可用的终端路径
```

### 7.2 配置文件 (config.ini)
```ini
[MT5]
terminal_path = D:\MetaTrader 5 EXNESS\terminal64.exe

[Alerts]
alert_distance_points = 5
alert_cooldown_sec = 120

[Target]
target_balance = 2000

[Display]
language = zh
```

### 7.3 复制到其他电脑
1. 复制整个 `黄金分析工具_Portable` 文件夹
2. 确保目标电脑安装 Python 3.12+
3. 安装依赖: `pip install MetaTrader5 matplotlib numpy`
4. 运行 `启动黄金分析助手.bat`
5. 首次运行自动检测 MT5 路径并生成 config.ini

---

## 八、文件结构

```
E:\MySoftware\黄金分析工具_Portable\
├── 黄金分析助手.py          # 主程序 (v2.2 简体中文版)
├── 黄金监控脚本.py          # 后台监控 (命令行版)
├── 启动黄金分析助手.bat     # 一键启动脚本
├── config.ini               # 配置文件 (自动生成)
├── 使用说明.md              # 用户文档
└── scripts/
    ├── detect_mt5.py        # MT5终端自动检测
    └── config_loader.py     # 配置加载器

桌面快捷方式:
├── 黄金分析助手.lnk  → 主程序
└── 黄金监控.lnk      → 监控脚本

Obsidian 笔记:
├── 黄金分析助手-项目记录.md
└── MT5-MQL5开发环境.md
```

---

## 九、目标追踪逻辑

```
目标: $2,000
当前余额: $384.70
差距: +$1,615.30 (420%)

持仓: 做多 0.5手 XAUUSDc @ 4406.217

计算公式:
  每点盈利 = 手数 × 合约规模 = 0.5 × 100 = 50 USC/点
  需要盈利点数 = 差距 ÷ 每点盈利 = 1615 ÷ 50 = 32.3 点
  目标金价 = 当前价 + 需要点数 = 4406 + 32 = ~4438

关键价位:
  阻力位: $4,476 (50周期高点)
  支撑位: $4,365 (50周期低点)
  当前距阻力: +70点 (+1.6%)
  当前距支撑: -41点 (-0.9%)
```

---

## 十、未来优化方向

### Phase 1 — 基础增强
- [ ] 添加 KDJ、ADX、OBV 等更多技术指标
- [ ] 优化图表渲染性能（减少重绘频率）
- [ ] 添加持仓盈亏预警（浮盈/浮亏达到阈值时提醒）

### Phase 2 — 智能功能
- [ ] 价格预警弹窗（触及支撑/阻力时）
- [ ] 历史回测功能（用历史数据验证策略）
- [ ] 多品种对比分析（同时监控多个品种）
- [ ] Telegram/微信消息推送

### Phase 3 — 自动化
- [ ] 策略编辑器（可视化配置EA参数）
- [ ] 一键部署EA到MT5
- [ ] 自动交易开关（手动/自动模式切换）
- [ ] 风控模块（最大持仓限制、单日亏损上限）

### Phase 4 — 高级分析
- [ ] 订单簿分析（Level 2数据）
- [ ] 资金流向监控
- [ ] 多时间框架共振分析
- [ ] AI辅助决策（大模型分析市场情绪）

---

## 十一、开发注意事项

### 编码规范
- 所有Python文件使用 **UTF-8 (无BOM)** 编码
- 中文 Windows 下避免使用 `Set-Content`/`Get-Content` 处理中文文件
- 使用 `[System.IO.File]::WriteAllText(path, content, [System.Text.UTF8Encoding]::new($false))` 写入

### MT5 连接要点
- `mt5.initialize(path)` 必须在使用任何MT5函数前调用
- 用完调用 `mt5.shutdown()` 释放连接
- 通过路径参数可指定不同终端实例
- 多个终端实例共享同一MT5数据目录

### 风险管理
- 快速交易按钮仅用于小仓位测试 (0.1/0.5手)
- 建议设置止损，控制单笔风险
- 不要过度杠杆（当前杠杆1:2000，建议谨慎使用）

---

*文档生成时间: 2026-09-08 23:35*  
*版本: v3.0 便携版*  
*下次更新: 根据MT5恢复情况测试交易功能*
---

## 清理记录

### 2026-09-08 清理内容
- 删除桌面旧版 黄金分析助手.py (v2.0，已迁移至便携版)
- 删除桌面 PNG 截图文件 (测试过程中产生)
- 删除 .codex/skills/mql5-dev/scripts/ 下临时调试脚本 (14个)

### 保留文件
- 桌面快捷方式 黄金分析助手.lnk 和 黄金监控.lnk (用户日常使用入口)
- E:\MySoftware\黄金分析工具_Portable\ (完整便携版)
- C:\Users\Administrator\Documents\HUANGJIN\黄金分析系统\架构设计文档.md (设计文档)
- C:\Users\Administrator\Documents\Obsidian Vault\黄金分析助手-项目记录.md (项目笔记)

---
---

## Bug 修复记录

### v2.2.1 - 2026-09-08
- 问题: 刷新时出现 The truth value of array is ambiguous 错误
- 原因: numpy 数组不能直接用 if array: 判断
- 修复: 全局替换所有 numpy 数组的 truth check
- 影响文件: 黄金分析助手.py
